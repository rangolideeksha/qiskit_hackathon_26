# Copy to .env and fill in. .env is git-ignored — never commit your token.
# Get a token at https://quantum.cloud.ibm.com/ (Account settings > API token).
QISKIT_IBM_TOKEN=
# Optional: instance CRN if using a specific IBM Quantum instance.
QISKIT_IBM_INSTANCE=
